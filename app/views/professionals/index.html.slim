h3.section-title  
  | C'MEMBERS
  small CEO MBA의 전문가와 기업들

= render '/professionals/divider'

.professionals-container.clear
  - @professionals.each do |professional|
    = link_to "#{professionals_path}/##{professional.id}", :class => "professional" do
      =image_tag(professional.profile_photo.url)
      .overlay
        h6.company 
          =professional.company.name
          br
          =professional.position
        h5.name =professional.name 

.reveal-modal

javascript:
  $(function() {
    hash_changed = function() {
      if (window.location.hash.length == 0)
        return;
      var professional_id = window.location.hash.substring(1);
      $modal_dom = $(".reveal-modal");
      $.get("#{professionals_path}/"+professional_id, function(r) {
        $modal_dom.html(r);
        $modal_dom.reveal({animation: "fade", animationspeed: 150});
      });
    }
    $(window).on('hashchange', hash_changed);
    hash_changed();
  
  });
