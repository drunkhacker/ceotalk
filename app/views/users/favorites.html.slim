hgroup class="session-page-header"
  h2 class="header-large" 즐겨찾기
  h4 class="header-small" CEO MBA에서 가치를 공유해보세요.

hr

h3.favorites.section-header C3 Talk
.favorite-items.clear
  - @talks.each do |talk|
    .item
      = image_tag(talk.thumb_url)
      a.overlay href="#{talk_path(talk)}"
        .category = talk.category
        h6.name= talk.professional.name
        h5.title= truncate(talk.title, length: 30,  omission: "...#{talk.title.last(10)}")
        p.description = truncate(strip_tags(talk.description), length: 120) 
  = paginate @talks
hr.section-divider


h3.favorites.section-header C3 Idea
.favorite-items.clear
  - @problems.each do |problem|
    .item
      = image_tag(problem.thumb_url)
      a.overlay href="#{problem_path(problem)}"
        h6.name= problem.professional.name
        h5.title= truncate(problem.title, length: 30,  omission: "...#{problem.title.last(10)}")
        p.description = truncate(strip_tags(problem.content), length: 120) 

  = paginate @problems
hr.section-divider


h3.favorites.section-header C3 Members
.favorite-items.clear
  - members = @members_favorite.map {|f| f.favorable_type.constantize.find(f.favorable_id)}
  - members.each do |member|
    .item
      = member.name
  = paginate @members_favorite
hr.section-divider

